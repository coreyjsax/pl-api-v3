<div>
    <style scoped>
        .ui.top.fixed.right.menu {
            height:30px;
        }
    </style>
    <script>
    
    </script>
    
    <div id="user-menu" class="ui top fixed right menu">
      <div class="item left"><a href="/"><strong>home</strong></a></div>
      <div class="item right" data-action="">
          <div class="ui inline dropdown">
            <div class="text">
                <img class="ui avatar image" src="<%=user.picture%>">
                <%=user.displayName%>
            </div>
            <i class="dropdown icon"></i>
            <div class="ui vertical menu">
              <div class="item">
                <%=user.title%> • <%=user.location%>
              </div>
              <a href="/logout" class="item" data-action="log-out">
                Log-out
              </a>
        </div>
          </div>
      </div>
    </div>
<script>
 /* function userMenuController(){
    let userMenu = document.getElementById('user-menu');
    userMenu.addEventListener('click', (event) => {
      let target = event.target;
      console.log(target)
      if (target.matches('.item')){
        let action = target.getAttribute('data-action');
        if (action === 'log-out'){
          
        }
      }
    })
    
  }
  userMenuController(); */
</script>